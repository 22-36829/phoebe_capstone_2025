# FAST DEPLOYMENT - Core packages only
# This will build in ~3-5 minutes instead of 15-20 minutes
# Use this for initial deployment, then add AI packages later

# Core Flask API (REQUIRED)
Flask==2.3.3
Flask-CORS==4.0.0
SQLAlchemy==2.0.21
psycopg2-binary==2.9.9
python-dotenv==1.0.1
Flask-JWT-Extended==4.6.0
bcrypt==4.2.0
gunicorn==21.2.0

# Data processing (REQUIRED for forecasting)
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0
requests==2.31.0

# Text processing (REQUIRED for AI search)
fuzzywuzzy==0.18.0
phonetics>=1.0.0
nltk>=3.8.1
textblob>=0.17.1

# Forecasting (REQUIRED)
statsmodels>=0.14.0
prophet>=1.1.5

# AI/ML packages - REQUIRED for AI Assistant
# CPU-only versions to reduce image size
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<3.0.0
sentence-transformers>=2.2.2
transformers>=4.35.0
faiss-cpu>=1.7.4

# Completely optional (not needed for basic API)
# spacy>=3.7.0
# networkx>=3.1
# plotly>=5.15.0
# dash>=2.14.0
# redis>=4.5.0
# celery>=5.3.0
# pytest>=7.4.0

