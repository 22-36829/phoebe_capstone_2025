# Core Flask API dependencies
Flask==2.3.3
Flask-CORS==4.0.0
SQLAlchemy==2.0.21
psycopg2-binary==2.9.9
python-dotenv==1.0.1
Flask-JWT-Extended==4.6.0
bcrypt==4.2.0
gunicorn==21.2.0

# Data processing (essential)
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0
requests==2.31.0

# Text processing (lightweight)
fuzzywuzzy==0.18.0
phonetics>=1.0.0
nltk>=3.8.1
textblob>=0.17.1

# ML/AI (CPU-only, smaller versions)
# Note: These are still large but necessary for AI features
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<3.0.0
sentence-transformers>=2.2.2
transformers>=4.35.0
faiss-cpu>=1.7.4

# Optional: Remove if not using
# spacy>=3.7.0  # Large, requires model downloads
# networkx>=3.1  # Only if using knowledge graphs
# plotly>=5.15.0  # Only if generating charts in API
# dash>=2.14.0  # Not needed for REST API
# redis>=4.5.0  # Only if using Redis caching
# celery>=5.3.0  # Only if using background tasks
# pytest>=7.4.0  # Not needed in production
# statsmodels>=0.14.0  # Only if doing advanced forecasting
# prophet>=1.1.5  # Large, only if using Prophet forecasting

